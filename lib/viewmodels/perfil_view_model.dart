import 'base_view_model.dart';

class PerfilViewModel extends BaseViewModel {
  // Placeholder for future profile-related logic (e.g., update display name,
  // change password, load stats). Currently, logout is handled at the View.
}
